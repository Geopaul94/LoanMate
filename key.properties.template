# Copy this file to `keystore.properties` (which is gitignored) and fill in
# your real values. NEVER commit the filled-in keystore.properties.
#
# storeFile is the path to your .jks keystore, relative to the project root.
storeFile=loanmate-release.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=loanmate
keyPassword=YOUR_KEY_PASSWORD
